void reverseString(char* s, int sSize) {
    char temp=0;
    int left=0;
    int right=sSize-1;
    while(left<right){
        temp=s[left];
        s[left]=s[right];
        s[right]=temp;
        left++;
        right--;
    }
}
